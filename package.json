{
  "name": "healthiq-proxy",
  "version": "1.0.0",
  "description": "Proxy server for HealthIQ Claude API integration",
  "main": "claude-proxy.js",
  "scripts": {
    "start": "node claude-proxy.js",
    "install-deps": "npm install express cors node-fetch@2"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}